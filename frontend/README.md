# GarageFlow Frontend

This frontend intentionally uses:

- HTML
- CSS
- TypeScript
- Vite

No React.

Run:

npm install
npm run dev

The Vite proxy sends `/api/*` requests to Spring Boot on port 8080.
